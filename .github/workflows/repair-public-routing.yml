name: Repair ECONOVA public routing

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate public entry
        run: test -f index.html && test -f visitor-gate.js
