name: Cleanup ECONOVA Public Homepage

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate public homepage
        run: test -f index.html
